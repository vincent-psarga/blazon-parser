import { Furs } from '../../../../../../domain/models/Tinctures';
import { tincture } from '../tincture';

/** A metal strewn with spots. A pelt, and a tincture in its own right. */
export const ermine = tincture(Furs.ermine);
